// This file is required by Expo Router but navigation is handled by _layout.tsx → RootNavigator.
// It renders nothing on its own since _layout.tsx takes over the entire screen.
export default function Index() {
  return null;
}
